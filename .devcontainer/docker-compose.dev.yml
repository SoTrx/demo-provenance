services:
  app:
    image: mcr.microsoft.com/devcontainers/python:3.14
    volumes:
      - ..:/workspaces:cached
    command: sleep infinity
    environment:
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=momademo
    depends_on:
      demo-provenance-postgres-provsql:
        condition: service_healthy